# This file is automatically generated by the rmf-codegen project.
#
# The Python code generator is maintained by Lab Digital. If you want to
# contribute to this project then please do not edit this file directly
# but send a pull request to the Lab Digital fork of rmf-codegen at
# https://github.com/labd/rmf-codegen
import typing
import warnings

from ...models.common import BuildUpload

if typing.TYPE_CHECKING:
    from ...base_client import BaseClient


class ApiBuildUploadRequestBuilder:

    _client: "BaseClient"

    def __init__(
        self,
        client: "BaseClient",
    ):
        self._client = client

    def post(
        self,
        body: "BuildUpload",
        *,
        headers: typing.Dict[str, str] = None,
        options: typing.Dict[str, typing.Any] = None,
    ) -> object:
        """Uploads your backend builds from your custom CI environment to the Studio."""
        headers = {} if headers is None else headers
        response = self._client._post(
            endpoint=f"/api/build/upload",
            params={},
            json=body.serialize(),
            headers={"Content-Type": "multipart/form-data", **headers},
            options=options,
        )
        if response.status_code == 200:
            return object.deserialize(response.json())
        warnings.warn("Unhandled status code %d" % response.status_code)
